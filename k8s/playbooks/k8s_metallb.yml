# ## Задача: Отдельный этап настройки MetalLB
# - **Статус**: В процессе
# - **Описание**: Выделить конфигурацию MetalLB в отдельный playbook.
- name: Configure MetalLB
  hosts: control_plane[0]
  become: true
  gather_facts: true
  roles:
    - role: k8s_metallb
      tags: [metallb, ingress]
